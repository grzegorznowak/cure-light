"""census — deterministic changed-range census tool unit (``changed-range-census/1``).

Stdlib + the ``git`` CLI only.  See ``SKILL.md`` for the operating recipe and
``DESIGN.md`` (tools root) section 5 for the normative schema.
"""

__version__ = "0.1.0"
